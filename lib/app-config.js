// ── App Configuration ──────────────────────────────────────────────────────────
// Edit these values to configure the app for your deployment.

// App identity — shown in nav header, setup screen splash, and browser tab
export var APP_NAME    = 'Meridian'
export var APP_TAGLINE = 'Intelligence Platform'

// Setup screen mode:
//   'production' — animated card-based wizard (for end users)
//   'dev'        — vertical form with all sections visible (for developers/testing)
export var SETUP_MODE = 'production'
